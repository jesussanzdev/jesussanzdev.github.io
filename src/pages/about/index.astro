---
import Section from "../../components/common/Section.astro";
import Layout from "../../layouts/Layout.astro";
import { GLOBAL } from "../../lib/variables";
---

<Layout>
  <Fragment slot="head">
    <title>{GLOBAL.aboutTitle} • {GLOBAL.username}</title>
    <meta
      name="description"
      content={GLOBAL.aboutShortDescription}
    />
    <meta property="og:title" content={`${GLOBAL.aboutTitle} • ${GLOBAL.username}`} />
    <meta
      property="og:description"
      content={GLOBAL.aboutShortDescription}
    />
    <meta property="og:image" content={`${GLOBAL.rootUrl}/${GLOBAL.aboutImage}`} />
    <meta property="og:url" content={`${GLOBAL.rootUrl}/about`} />
    <meta http-equiv="content-language" content="en" />
    <meta name="language" content="English" />
    <link rel="canonical" href={`${GLOBAL.rootUrl}/about`} />
  </Fragment>

<Section class="py-4 my-8">
  <div class="flex items-center gap-4 pt-8 pb-16">
    <h1 class="font-display text-3xl sm:text-4xl leading-loose">{GLOBAL.aboutName}</h1>
  </div>

  <div class="flex flex-col md:flex-row items-center md:items-start gap-10 mb-8">
    <img 
      src={`${GLOBAL.rootUrl}/${GLOBAL.aboutImage}`} 
      alt="Profile Picture" 
      class="w-80 h-auto shadow-lg rounded-lg md:mt-1"
    />

    <div class="prose prose-lg dark:prose-invert max-w-none">
      <p>
        I’m passionate about nature and the outdoors, finding energy and inspiration through activities like hiking, surfing, and exploring natural landscapes. Physical activity is central to my life, helping me stay balanced and connected.
      </p>
    </div>
  </div>
  
  <div class="prose prose-lg dark:prose-invert max-w-none">
    <p>
      Beyond nature, I’m a film enthusiast who loves the art of storytelling, from classic cinema to modern masterpieces. I’m equally fascinated by immersive digital experiences like virtual reality, and I stay curious about emerging technologies that shape how we live, connect, and experience the world.
    </p>
  </div>
</Section>
</Layout>
